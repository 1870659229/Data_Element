任务需求



本项目旨在利用海量AIS轨迹数据，通过数据挖掘与算法优化，构建一套支撑船舶智能辅助驾驶的高精度底层数据集，并实现基于船舶物理特性的定制化路径规划解决方案。



\### 具体任务要求：



\*\*1. 航道拓扑节点网络提取\*\*

\- 通过清洗、平滑海量原始GPS数据（去除异常漂移点）

\- 从真实轨迹中拟合、聚类出现实中的高频航行节点（如真实航道中的拐点、分岔点、汇合点）

\- 大幅度降低路径规划时的数据冗余量

\- 构建标准化的水上航道拓扑网络数据集



\*\*2. 动态路段耗时权重建模\*\*

\- 针对任意两个相邻节点，不单纯依赖物理直线距离

\- 基于历史GPS轨迹数据（结合速度、方向等特征）

\- 拟合出更符合实际水面环境的相邻节点耗时权重

\- 形成高质量的特征标签



\*\*3. 路径规划算法设计与实现\*\*

依托构建的高质量数据集，设计或改进路径规划算法（如A\*、Dijkstra的改进版），实现针对特定船舶的个性化导航决策：



\- \*\*特征属性检索\*\*：算法需根据目标船舶的身份信息，通过网络查询或关联数据库获取其船长、船宽、船高、载重吨位及满载吃水深度等关键静态特征

\- \*\*物理约束校验\*\*：在规划过程中，必须充分考虑船舶物理限制。例如，大型船舶需规避吃水深度不足的浅滩，高箱船需避开限高不足的桥梁航段

\- \*\*多目标路径输出\*\*：针对给定的起终点，快速输出2-3条差异化干预路径（如：安全优先线路、时间最短线路、通航频次最高的综合最优线路），为智能驾驶UI/UX界面提供精准决策支持



\*\*算法输入和输出：\*\*

\- 输入：船舶起点位置（GPS坐标）和船舶终点位置（GPS坐标）

\- 输出：2-3条规划路径，如：距离最短线路，时间最短线路，综合最优线路，并输出相应线路的节点



\## 预期技术指标

\- 轨迹数据平滑

\- 提取区域内主要水上航道节点  

\- 规划输出的综合最优路径



